# Legacy config: editor deployment now targets Vercel (`vercel.json`).
[build]
  command = "npm run build:editor"
  publish = "dist"

[build.environment]
  SITE_MODE = "editor"
  HOSTING_PROVIDER = "netlify"

[[headers]]
  for = "/*"
  [headers.values]
    X-Robots-Tag = "noindex, nofollow"

